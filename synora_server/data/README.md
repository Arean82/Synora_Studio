# Server Data Directory

This folder is dynamically generated at runtime.
It securely holds all:
- `vector_db`
- `conversations`
- `saas_tenants.db`
- `config.ini`

Because the app is modular, this folder replaces the old project root storage.
